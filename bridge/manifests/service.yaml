apiVersion: v1
kind: Service
metadata:
  name: opencode-bridge
  namespace: opencode
  labels:
    app: opencode-bridge
spec:
  type: ClusterIP
  selector:
    app: opencode-bridge
  ports:
    - name: http
      port: 3100
      targetPort: 3100